apiVersion: v1
kind: Service
metadata:
  name: smartops-service
spec:
  selector:
    app: smartops-app
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 30000